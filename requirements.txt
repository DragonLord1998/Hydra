flask
torch
torchvision  # must match torch version (e.g. torch 2.10 → torchvision 0.25)
Pillow
accelerate
sentencepiece
transformers
numpy
blinker
huggingface_hub
# diffusers — install from git main for Flux2Pipeline + from_single_file:
# pip install git+https://github.com/huggingface/diffusers.git
